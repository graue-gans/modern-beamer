# Express-Beamer fork to be used as slides template repository

To remove the original + the template repository's git history run and initialize fresh local git, run the following script provided with the template
`chmod +x .git-cleanup.sh && ./git-cleanup.sh`
